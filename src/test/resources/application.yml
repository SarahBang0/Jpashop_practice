spring:
#  datasource:
#    url: jdbc:h2:mem:testdb // 이부분으로 메모리DB 설정하는 거임
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
#        #        show_sql:true
#        format_sql: true
# 사실 위에 코드 다 없어도 됨.

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: trace